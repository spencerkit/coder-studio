# 产品需求文档：Coder Studio

> **版本：** 0.2.6  
> **文档日期：** 2026 年 4 月 13 日  
> **产品类型：** 本地优先 AI 编码工作台

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户](#2-目标用户)
3. [核心价值主张](#3-核心价值主张)
4. [信息架构](#4-信息架构)
5. [全局 UI 框架](#5-全局-ui-框架)
6. [认证系统](#6-认证系统)
7. [工作区管理](#7-工作区管理)
8. [Agent 会话管理](#8-agent-会话管理)
9. [代码编辑器与文件浏览](#9-代码编辑器与文件浏览)
10. [Git 集成](#10-git-集成)
11. [终端系统](#11-终端系统)
12. [命令面板](#12-命令面板)
13. [设置](#13-设置)
14. [专注模式](#14-专注模式)
15. [通知系统](#15-通知系统)
16. [Supervisor 系统](#16-supervisor-系统)
17. [国际化](#17-国际化)
18. [设计系统](#18-设计系统)
19. [交互模式](#19-交互模式)
20. [边界情况与错误状态](#20-边界情况与错误状态)

---

## 1. 产品概述

### 1.1 产品定义

Coder Studio 是一款本地优先的 AI 编码工作台，将 AI 驱动的代码生成 Agent、代码编辑、版本控制和终端访问整合到一个统一的开发者界面中。它使开发者能够在单个工作区内编排 AI 编码 Agent（Claude、Codex），并与传统开发工具协同工作。

### 1.2 产品愿景

为开发者提供一个集中式工作台，让 AI Agent 和人类开发者能够实时协作编写代码，并对 Agent 活动、文件变更和终端输出保持完全可见。

### 1.3 核心特征

- **本地优先：** 运行在用户本机；所有代码、终端和 Agent 进程均在本地执行。
- **多 Agent 支持：** 支持在单个工作区内并行运行多个 AI 编码 Agent 会话。
- **全栈开发环境：** 将代码编辑、Git 操作、终端访问和 Agent 编排集成在一个界面中。
- **抗干扰：** 提供专注模式，隐藏非必要的 UI 元素，让用户可以集中注意力工作。

---

## 2. 目标用户

### 2.1 主要用户

**专业软件开发人员**，他们在日常工作中使用 AI 编码助手（Claude Code、Codex），需要一个专用环境来在代码、终端和版本控制之间管理多个 Agent 会话。

### 2.2 次要用户

- **技术负责人**：审查 AI 生成的代码和 Agent 进度。
- **DevOps 工程师**：管理带有 AI 辅助的本地开发环境。

---

## 3. 核心价值主张

| 需求 | 解决方案 |
|------|----------|
| 同时管理多个 AI Agent 会话 | 多面板 Agent 工作区，支持并行会话 |
| 实时查看 Agent 的动态 | 每个 Agent 嵌入独立终端，实时输出流 |
| 审查和编辑 Agent 生成的代码 | 集成代码编辑器，支持文件浏览和差异对比 |
| 追踪和提交 Agent 变更 | Git 集成，支持暂存、差异审查和就地提交 |
| 在 Agent 工作旁运行命令 | 多标签终端面板，包含 Agent 专属终端和 Shell 终端 |
| 在 Agent 长时间运行时保持专注 | 专注模式、完成通知、Supervisor 目标追踪 |

---

## 4. 信息架构

```
Coder Studio
├── 认证页面（如已启用）
├── 应用层
│   ├── 顶栏（全局，打开工作区时始终可见）
│   └── 内容区域
│       ├── 欢迎页面（未打开任何工作区时）
│       ├── 工作区页面（活跃工作区）
│       │   ├── 左侧面板：代码侧边栏（文件 / Git 差异）
│       │   ├── 中央面板：Agent 工作区
│       │   └── 底部面板：终端
│       └── 设置页面
└── 浮层
    ├── 命令面板
    ├── 工作区启动浮层
    ├── 运行时校验浮层
    ├── 确认对话框
    ├── Worktree 弹窗
    └── Supervisor 目标对话框
```

### 4.1 路由结构

| 路由 | 说明 |
|------|------|
| `/` | 用于启动或重新进入工作区的欢迎页 |
| `/workspace` | 主工作区页面；当前激活工作区由前端状态解析 |
| `/settings` | 设置页面 |

---

## 5. 全局 UI 框架

### 5.1 顶栏

顶栏是应用顶部的一条持久水平条（高度 36px），只要至少有一个工作区打开就会显示。

#### 5.1.1 布局

顶栏分为两个区域：

- **左侧区域：** 工作区/会话标签
- **右侧区域：** 操作按钮

#### 5.1.2 工作区/会话标签

每个打开的工作区用一个标签表示，包含以下元素（从左到右）：

1. **状态指示点：**
   - 工作区活跃运行时：彩色圆点带有持续的脉冲动画（透明度每 2 秒在 1.0 和 0.5 之间振荡）。
   - 空闲时：静态彩色圆点，无动画。
   - 颜色映射：活跃 = 绿色（`#78d7b2`），空闲 = 灰蓝色。

2. **标签文本：** 显示工作区名称。过长时以省略号截断。

3. **未读徽标（条件显示）：** 显示该工作区未读消息的数量。以数字显示（最大显示为 "9+"），位于小型圆角徽标内（最小宽 16px，高 16px），背景为强调蓝色。数量为零时隐藏。

4. **关闭按钮（X）：** 默认隐藏，鼠标悬停在标签上时出现。点击关闭该工作区标签。

5. **添加按钮（+）：** 28×28px 的加号图标按钮。打开新的工作区标签（启动工作区启动浮层）。

#### 5.1.3 标签交互

- **切换标签：** 点击任意标签切换到该工作区。
- **关闭标签：** 将鼠标悬停在标签上显示 X 按钮，点击关闭。
- **新建标签：** 点击 + 按钮打开新工作区（打开工作区启动浮层）。

#### 5.1.4 右侧操作按钮

两个 32×32px 的圆角按钮（6px 半径）：

1. **快速操作按钮：** 显示搜索图标和文字 "Quick Actions"。打开命令面板。在较宽屏幕上显示文字标签，较窄屏幕只显示图标。

2. **设置按钮：** 显示齿轮图标。导航到设置页面。带有数据属性 `data-testid="settings-open"`。

两个按钮共享相同的悬停状态：背景变为稍亮的色调。

#### 5.1.5 空状态

没有工作区打开时，顶栏显示居中空状态，包含一行小标题和提示文字，引导用户打开工作区。

#### 5.1.6 设置路由下的顶栏外观

在设置页面时，顶栏变为：
- **左侧：** 返回按钮，带有返回箭头图标和文字 "Back to App"。
- **居中：** 标题 "Settings"。

### 5.2 全局快捷键

| 快捷键 | 操作 |
|--------|------|
| `Ctrl/Cmd + K` | 切换命令面板 |
| `Escape`（命令面板打开时） | 关闭命令面板 |
| `Ctrl/Cmd + S` | 保存代码编辑器中当前打开的文件 |
| `Ctrl/Cmd + N` | 打开新工作区标签 |
| `Ctrl/Cmd + Shift + [` | 切换到上一个工作区标签 |
| `Ctrl/Cmd + Shift + ]` | 切换到下一个工作区标签 |
| `F`（无修饰键，且未聚焦于文本输入框） | 切换专注模式 |
| `Escape`（在专注模式下） | 退出专注模式 |
| `Cmd + D`（Mac）或 `Alt + D`（Windows/Linux） | 垂直分割当前 Agent 面板 |
| `Cmd + Shift + D`（Mac）或 `Alt + Shift + D`（Windows/Linux） | 水平分割当前 Agent 面板 |

**注意：** 快捷键具有上下文感知能力。`F` 键仅在用户未主动在文本输入框中打字时才切换专注模式。保存快捷键（`Ctrl/Cmd + S`）仅在代码编辑面板可见且有文件打开时生效。

### 5.3 色彩体系（深色主题 — "Aurora Mint"）

| 变量 | 值 | 用途 |
|------|-----|------|
| Background（背景） | `#0a1014` | 应用主背景 |
| Surface（表面） | `#11181f` | 面板、卡片、提升表面 |
| Border（边框） | `#1e2a35` | 分割线和边框 |
| Text Primary（主文本） | `#e5edf3` | 标题、标签、正文 |
| Text Secondary（次要文本） | `#9fb0bc` | 次要标签、描述文字 |
| Text Tertiary（三级文本） | `#728492` | 小标题、时间戳、弱化标签 |
| Accent（强调色-蓝） | `#6cb6ff` | 链接、活跃状态、高亮 |
| Accent-2（辅助色-绿） | `#78d7b2` | 成功指示、运行状态 |
| Accent-3（辅助色-琥珀） | `#f1b86a` | 警告、修改状态 |
| Danger（危险色-粉） | `#ff9eb0` | 错误、破坏性操作、删除状态 |

### 5.4 字体规范

| 级别 | 字号 | 字重 | 用途 |
|------|------|------|------|
| 小标题 | 10px | 大写，字间距 0.08em | 分区标签、徽标 |
| XS | 11px | 常规/中等 | Agent 面板标题、文件路径 |
| SM | 12px | 常规 | 按钮文字、小标签 |
| MD | 13px | 常规 | 正文、编辑器内容、终端文字 |
| LG | 14px | 常规 | 标题、表单标签 |
| 3XL | 18px | 中等 | 欢迎页标题 |

- **UI 字体：** IBM Plex Sans
- **等宽字体：** JetBrains Mono（用于代码编辑器、终端和代码块）

### 5.5 图标规范

所有图标均来自 Lucide 图标库，默认渲染为 16px，线宽 1.5px。产品中使用的关键图标：

| 图标 | 名称 | 用途 |
|------|------|------|
| `Plus` | HeaderAddIcon | 新建工作区、新建终端 |
| `X` | HeaderCloseIcon | 关闭标签、关闭面板、关闭弹窗 |
| `Search` | SearchIcon | 命令面板、文件搜索 |
| `Settings` | HeaderSettingsIcon | 设置按钮/链接 |
| `ArrowLeft` | HeaderBackIcon | 返回导航 |
| `ChevronRight` / `ChevronDown` | 展开/折叠 | 文件树、手风琴 |
| `RefreshCw` | RefreshIcon | 刷新文件树、刷新 Git、重试校验 |
| `Plus`（Git 上下文中） | GitStageIcon | 暂存变更 |
| `Minus` | GitUnstageIcon | 取消暂存 |
| `Undo2` | GitDiscardIcon | 丢弃变更 |
| `ArrowUp` | AgentSendIcon | 提交、触发 Supervisor |
| `Square` | 停止/禁用 | 禁用 Supervisor |
| `CirclePause` / `Play` | 暂停/恢复 | Supervisor 暂停/恢复 |
| `MessageSquare` | 编辑目标 | 编辑 Supervisor 目标 |
| `BadgeCheck` | Supervisor 徽章 | Supervisor 分区 |
| `Folder` | WorkspaceFolderIcon | 文件夹浏览器、文件树 |
| `GitBranch` | WorkspaceBranchIcon | 分支显示 |
| `SplitHorizontal` / `SplitVertical` | 面板分割图标 | 分割 Agent 面板 |

### 5.6 动画与过渡规范

| 属性 | 值 | 用途 |
|------|-----|------|
| 快速时长 | 60ms | 微交互、按钮状态 |
| 普通时长 | 100ms | 标准过渡 |
| 慢速时长 | 150ms | 面板缩放器、较大移动 |
| Ease-out-expo | `cubic-bezier(0.16, 1, 0.3, 1)` | 弹窗出现 |
| Ease-out-quart | `cubic-bezier(0.25, 1, 0.5, 1)` | 通用 UI 过渡 |

**关键帧动画：**

- **脉冲（Pulse）：** 持续透明度振荡（1.0 → 0.5 → 1.0），周期 2 秒。用于运行状态指示点。
- **淡入（Fade In）：** 透明度 0 → 1。用于弹窗遮罩。
- **缩放入（Scale In）：** 缩放 0.95 → 1.0，透明度 0 → 1。用于弹窗卡片。
- **滑入（Slide In）：** X 轴从 100% → 0，带透明度。用于 Toast 通知。
- **旋转（Spin）：** 360 度旋转，周期 0.8 秒，线性，无限循环。用于加载旋转器和检查指示器。
- **闪烁（Shimmer）：** 背景位置偏移，周期 1.5 秒。用于骨架屏加载器。
- **淡入上移（Fade In Up）：** Y 轴从 10px → 0，带透明度。用于列表项出现。
- **右滑入（Slide In Right）：** X 轴从 20px → 0，带透明度。用于面板内容。

**性能规则：** 在面板调整操作期间，面板内容上的所有过渡和动画会临时禁用，以保持流畅的调整体验。

---

## 6. 认证系统

### 6.1 用途

对于暴露在网络上（远程服务器、共享机器）的部署，认证系统提供单密码访问控制，防止未经授权的访问。

### 6.2 认证流程

#### 6.2.1 状态

认证页面经历以下状态：

1. **加载中：** 确定认证状态时显示旋转加载器。
2. **未配置：** 如果服务器未设置认证，告知用户应用在没有认证的情况下运行。
3. **登录：** 标准登录界面，带有密码短语输入框。
4. **已封锁：** 用户 IP 因多次失败尝试被锁定。
5. **不可用：** 无法连接到认证服务。
6. **解锁中：** 成功认证过程中的过渡状态。

#### 6.2.2 登录页面

登录页面包含：

- **产品品牌：** 产品名称和版本，显示在顶部。
- **密码输入框：** 文本输入框，占位符提示输入密码短语。
- **提交按钮：** 主要操作按钮，标签为 "Sign In"。
- **错误消息（条件显示）：** 当凭证无效时在输入框下方显示，使用危险色样式。
- **安全信息卡片**（登录表单下方）：
  - **会话策略：** 显示会话空闲超时时间和最大会话生命周期。
  - **允许的路径：** 列出已认证会话允许访问的目录路径。

#### 6.2.3 预览外壳

在认证页面时，登录卡片后方会显示工作台 UI 的线框预览。此预览由占位条和块组成，模拟顶栏、左侧边栏、主内容区域和右侧面板，让用户在登录后能看到界面样式的视觉暗示。

### 6.3 安全策略

| 策略 | 详情 |
|------|------|
| 失败尝试锁定 | 10 分钟内 3 次失败登录尝试将导致 IP 被封禁 24 小时 |
| 会话 Cookie | 基于 HttpOnly Cookie 的会话，防止基于 XSS 的令牌窃取 |
| 会话空闲超时 | 会话在可配置的非活动时间后终止 |
| 最大会话时长 | 会话有硬生命周期限制，超时后无论是否活跃都会过期 |

### 6.4 错误码

| 错误码 | 用户可见消息 |
|--------|-------------|
| `invalid_credentials` | "密码不正确" |
| `session_expired` | "您的会话已过期，请重新登录。" |
| `session_missing` | "没有活跃的会话，请登录。" |
| `ip_blocked` | "您的 IP 因多次失败尝试已被封锁。" |
| `auth_unavailable` | "认证服务当前不可用。" |
| `auth_not_configured` | "此部署未配置认证。" |

---

## 7. 工作区管理

### 7.1 概述

一个工作区代表一个正在积极开发的项目目录。每个工作区包含自己的一组 Agent 会话、文件浏览器、Git 状态和终端。多个工作区可以作为顶栏中的独立标签同时打开。

### 7.2 工作区启动

#### 7.2.1 触发方式

点击顶栏中的 "+" 按钮或欢迎页面上的 "Open Workspace" 按钮，打开工作区启动浮层。

#### 7.2.2 工作区启动浮层

一个模态对话框（最大宽度 760px），包含以下分区：

**头部区域（两列布局）：**
- **左列：**
  - 小标题："START WORKSPACE"
  - 标题："Local Folder"
  - 提示文字，解释选择
- **右列：**
  - 当前选中路径显示
  - 执行目标指示器（Native / WSL）
  - 关闭按钮（X）

**选择卡片：**
- **本地文件夹（始终可用）：** 从本地目录创建工作区的主要选项。
- **远程 Git（已延期）：** 视觉禁用/非活动选项，表明此功能已计划但当前不可用。

**执行目标选择：**
- **Native：** 默认选项。直接在主机操作系统上运行 Agent。
- **WSL（Windows Subsystem for Linux）：** 在 Windows 上可用的替代选项。选择 WSL 会显示一个额外的文本输入框，用于指定 WSL 发行版名称（可选；默认为系统默认发行版）。

**文件夹浏览器：**
一个文件选择器界面，包含：

1. **工具栏：**
   - **主目录按钮：** 导航到用户的主目录。
   - **向上按钮：** 导航到父目录。

2. **根路径快捷 chips：** 常用根目录的快速访问按钮（如根目录 `/`、常见项目目录）。点击 chip 直接导航到该路径。

3. **目录列表：**
   - 每行显示文件夹图标、目录名和简短提示（如修改时间或项目数）。
   - 悬停时出现"进入文件夹"操作（箭头图标）。
   - 点击目录行导航进入该目录。
   - 点击目录名称（非操作按钮）选中为工作区根目录。

**选中路径显示：**
- 以面包屑样式文字显示当前选中的目录路径。
- 用户浏览文件夹浏览器时实时更新。

**操作按钮：**
- **启动工作区：** 浮层底部的一个主要按钮。在选中有效目录路径之前为禁用状态。点击此按钮创建并打开新工作区标签。

#### 7.2.3 运行时校验

选择目录和执行目标后，系统执行运行时校验（见第 7.3 节）。如果所选目标上缺少所需命令，工作区将无法启动。

### 7.3 运行时校验浮层

在启动工作区时自动弹出的模态对话框，用于验证所选执行目标上所有必需的系统命令是否可用。

#### 7.3.1 布局

- **头部：**
  - 小标题："RUNTIME CHECK"
  - 标题：上下文消息（如 "正在检查可用命令"）
  - 描述：解释正在检查的内容
  - 关闭按钮（X）

- **目标选择（条件显示）：** 如果有可用的 WSL 发行版，用户可以选择 "Native" 或 "WSL"。选择 WSL 会显示发行版名称的文本输入框。

- **所需运行时摘要卡片：** 一张卡片列出当前配置所需的所有命令。

- **需求列表：** 每个所需命令列出：
  - **状态点：** 绿色=可用，红色=缺失，蓝色带脉冲动画=正在检查。
  - **命令标签：** 命令名称（如 `git`、`node`）。
  - **详情文字：** 附加信息（如 "found at /usr/bin/git" 或 "not found"）。

- **安装提示（条件显示）：** 当需求未满足时，显示消息建议如何安装缺失命令。

- **操作按钮：**
  - **重试：** 仅在校验状态为 "failed" 时启用。点击重新运行检查。

- **关闭：** 浮层可以用 Escape 键或 X 按钮关闭。

#### 7.3.2 校验状态

| 状态 | 描述 |
|------|------|
| 空闲 | 校验尚未触发 |
| 检查中 | 校验进行中（蓝色脉冲指示器） |
| 就绪 | 所有所需命令可用（绿色指示器） |
| 失败 | 一个或多个所需命令缺失（红色指示器） |

### 7.4 欢迎页面

在未打开任何工作区标签时显示。

#### 7.4.1 布局

- **居中面板：**
  - 顶部小标题
  - H1 标题（如 "Coder Studio"）
  - 描述产品的正文段落
  - **主要操作按钮：** 标签 "Open Workspace"，带加号图标。点击打开工作区启动浮层。
  - **设置链接：** 带齿轮图标的文字链接，标签 "Open Settings"。点击导航到设置页面。

### 7.5 工作区生命周期

| 操作 | 行为 |
|------|------|
| 打开 | 创建新标签，初始化 Agent 面板、文件树和终端 |
| 切换 | 保留所有状态（Agent 会话、终端输出、编辑器状态）；工作区在后台继续运行 |
| 关闭 | 终止所有 Agent 会话，关闭终端，移除标签 |
| 恢复 | 如果应用在工作区活跃时重新加载，工作区尝试恢复之前的状态 |

### 7.6 多标签并发控制

当应用在多个浏览器标签页中打开时，控制器/观察者模型管理哪个标签页对工作区拥有主要控制权：

- 一个标签页作为**控制器**并持有隔离令牌。
- 其他标签页是**观察者**，接收更新但不能直接修改工作区状态。
- 控制器定期发送心跳（可见时每 10 秒，标签隐藏时每 20 秒）。
- 如果控制器无响应，观察者可以请求接管，采用基于截止时间的交接协议。
- 标签页关闭或导航离开时，控制器自动释放。

---

## 8. Agent 会话管理

### 8.1 概述

Agent 会话是工作区的核心交互单元。每个会话在自己的进程中运行一个 AI 编码 Agent（Claude 或 Codex），并带有专用的终端用于输入/输出。多个 Agent 会话可以在单个工作区内并行运行，以分割面板布局排列。

### 8.2 Agent 面板状态

每个 Agent 面板可以处于以下状态之一：

| 状态 | 视觉表现 |
|------|----------|
| **草稿** | 启动卡片，带有 Provider 选择按钮（Claude、Codex）。尚未启动。 |
| **运行中** | 活跃终端，流式输出。状态点脉冲绿色。进度条动画。 |
| **空闲** | 终端可见但未主动产生输出。状态点为静态。 |
| **已中断** | 会话被手动中断。状态指示器反映中断状态。 |
| **不可用** | 错误状态。显示错误消息、原因和"移除"按钮。 |

### 8.3 Agent 面板布局

#### 8.3.1 面板卡片结构

每个 Agent 会话显示在卡片内（`.agent-pane-card`），具有以下垂直结构：

1. **进度条：** 卡片最顶部的一条细线（3px 高度）。
   - "live" 或 "loading" 状态为强调蓝色。
   - "error" 状态为危险粉色。
   - 宽度动态变化：最小 14%，活跃运行时 34%，否则 6%。

2. **面板头部：** 紧凑头部，包含：
   - **会话点：** 小彩色点，指示状态（绿色 = 活跃，灰色 = 空闲，蓝色 = 排队，弱化 = 关闭）。
   - **标题：** 大写，11px，等宽字体，字间距 0.5px。显示会话标识符。
   - **Provider 徽章：** 小标签显示 Agent Provider 名称（如 "Claude"、"Codex"）。
   - **状态标签：** 小标签显示当前会话状态（如 "Running"、"Idle"、"Queued"）。
   - **操作按钮**（右对齐，悬停时可见）：
     - 垂直分割图标
     - 水平分割图标
     - 关闭（X）图标——悬停时变红

3. **终端区域：** 嵌入终端（xterm.js），填充卡片剩余空间。支持交互模式（用户可以输入命令）和只读模式（仅输出）。

#### 8.3.2 分割面板布局

- **垂直分割：** 将中央面板分为左右两个面板。由垂直分割按钮或 `Cmd/Ctrl + D` 触发。
- **水平分割：** 将中央面板分为上下两个面板。由水平分割按钮或 `Cmd/Ctrl + Shift + D` 触发。
- **调整大小：** 分割分隔条宽 8px，可拖动。拖动调整相邻面板的相对大小。
- **嵌套分割：** 面板可以递归分割，允许在单个工作区内进行复杂的多面板布局。

### 8.4 草稿会话启动

创建新面板时（通过 "+" 操作或分割），它以**草稿**面板开始：

- 显示启动卡片（`.agent-draft-launcher`），带有每个可用 Provider 的按钮。
- 每个 Provider 按钮显示 Provider 的徽章标签和显示名称。
- 点击 Provider 按钮启动该 Provider 的新 Agent 会话。
- 选择 Provider 后，草稿面板被活跃会话面板替换。

### 8.5 会话追踪

每个活跃会话追踪以下元数据：

| 属性 | 描述 |
|------|------|
| 状态 | 当前生命周期状态（idle、running、interrupted） |
| 模式 | 操作模式（branch 或 git_tree） |
| 恢复 ID | 用于恢复之前会话的标识符 |
| 最后活跃时间 | 最近活动的时间戳 |
| 完成率 | 完成进度，以进度条显示 |

### 8.6 会话生命周期事件

会话发出以下生命周期事件：

| 事件 | 触发时机 |
|------|----------|
| `session_started` | 新 Agent 进程启动时 |
| `turn_completed` | Agent 完成一轮编码请求时 |

### 8.7 会话恢复

- **归档：** 过去的会话被记录，可以在归档历史中查看。
- **恢复：** 可以使用恢复 ID 恢复之前的会话，恢复上下文。
- **移除：** 不可用或出错的会话可以从工作区中移除。

### 8.8 空闲策略

每个工作区维护一个每工作区级别的空闲策略：

| 设置 | 描述 |
|------|------|
| 是否启用 | 空闲策略是否激活 |
| 空闲分钟数 | 无活动多少分钟后将会话标记为空闲 |
| 最大活跃会话数 | 进入压力模式前允许的最大会话数 |
| 压力模式 | 同时运行过多会话时的行为 |

会话在配置的空闲时间后被标记为空闲。当空闲会话完成工作时，可以触发完成提醒。

---

## 9. 代码编辑器与文件浏览

### 9.1 左侧面板（代码侧边栏）

左侧面板是一个可调整大小的侧边栏（默认宽度 280px，最小 200px，最大 400px），带有深色表面背景（`#0d141a`）和右边框。它包含两个可切换的视图：**文件** 和 **Git 差异**。

### 9.2 视图切换

在侧边栏顶部，一个标签栏允许在视图之间切换：

- **文件标签：** 显示仓库文件树。
- **Git 差异标签：** 显示 Git 变更面板。

两个标签均为 24px 高，4px 圆角。活跃标签视觉高亮。

### 9.3 文件树视图

#### 9.3.1 头部

- **分区标签：** "REPOSITORY NAVIGATOR"，大写，10px，宽字间距。
- **分支 Chip：** 显示当前 Git 分支名称或根路径，带有文件夹或分支图标。

#### 9.3.2 工具栏

单个刷新按钮，重新扫描工作区目录并更新文件树。

#### 9.3.3 树结构

- **文件夹：** 显示文件夹图标和表示展开/折叠状态的箭头（向右 = 折叠，向下 = 展开）。
- **文件：** 显示文件类型彩色图标和文件名。
- **选中状态：** 当前选中的文件左边框带有强调色。
- **折叠状态：** 目录的展开/折叠状态按工作区持久化。

#### 9.3.4 交互

- **点击文件夹：** 展开或折叠文件夹以显示/隐藏其内容。
- **点击文件：** 在代码编辑器面板（中央左侧区域）中打开文件。
- **展开/折叠：** 点击箭头切换文件夹可见性，不选中。

### 9.4 文件搜索

#### 9.4.1 搜索字段

位于代码编辑器面板头部顶部：

- **搜索图标：** 输入框左侧。
- **输入框：** 最大宽度 360px。占位文字："Search files..."
- **功能：** 用户输入时实时过滤工作区文件树。

#### 9.4.2 搜索结果下拉

- 出现在搜索框下方的浮动下拉框（`.workspace-search-dropdown.floating`）。
- 列出匹配的文件及其名称和父路径。
- 当前聚焦的结果高亮显示。
- **键盘导航：** 上箭头/下箭头移动焦点，Enter 选中，Escape 关闭。

### 9.5 代码编辑器面板

#### 9.5.1 布局

代码编辑器占据中央区域的左侧，与 Agent 面板相邻。

**头部：**
- **文件路径显示：** 显示当前打开文件的路径（11px，等宽，最多 56 个字符）。过长时以省略号截断。
- **搜索字段：** 上述文件搜索输入。

**主体：**
- **展开状态：** 右侧显示 Monaco 代码编辑器，左侧显示文件信息侧边栏。
- **折叠状态：** 只显示侧边栏内容，不显示编辑器。

#### 9.5.2 编辑器模式

- **预览模式：** 标准代码查看，带语法高亮和行号。
- **差异模式：** Monaco 差异编辑器，显示原始内容与修改内容的并排对比。

用户可以通过编辑器工具栏中的切换按钮在预览和差异模式之间切换。

#### 9.5.3 编辑器交互

- **语法高亮：** 基于文件扩展名自动语言检测。
- **保存：** `Ctrl/Cmd + S` 保存当前文件。
- **字号：** 编辑器中 13px。
- **内边距：** 编辑器内容区域周围 12px。

---

## 10. Git 集成

### 10.1 Git 变更面板

通过左侧边栏的 "Git Diff" 标签访问。

#### 10.1.1 头部

- **分区标签：** "SOURCE CONTROL"，大写，10px，宽字间距。
- **分支 Chip：** 显示当前 Git 分支名称，带有分支图标。

#### 10.1.2 工具栏

五个操作按钮（从左到右）：

1. **刷新：** 重新扫描 Git 状态并更新变更列表。
2. **全部暂存：** 暂存所有未暂存的变更。
3. **全部取消暂存：** 取消暂存所有已暂存的变更。
4. **全部丢弃：** 丢弃所有未暂存的变更（触发确认对话框）。
5. **提交：** 提交已暂存的变更。未输入提交信息时禁用。

#### 10.1.3 提交信息输入

工具栏下方的文本区域（最小高度 30px），用于输入提交信息。

#### 10.1.4 变更分组

变更按三个分组组织：

1. **已暂存：** 已为下次提交暂存的文件。
2. **变更：** 已修改但尚未暂存的文件。
3. **未跟踪：** Git 尚未跟踪的新文件。

每个分组有一个头部，显示分组标签和其中的项目数。

#### 10.1.5 变更行

每个文件变更显示为一行，包含：

- **文件图标：** 基于文件类型着色。
- **文件名：** 变更文件的名称。
- **父路径：** 文件所在的目录。
- **状态徽标：** 彩色徽标，指示变更类型：
  - 已暂存：绿色（`#78d7b2`）
  - 已修改：橙色（`#f1b86a`）
  - 未跟踪：蓝色（`#6cb6ff`）
  - 已删除：红色（`#ff9eb0`）
- **操作按钮（每个文件）：** 暂存、取消暂存或丢弃（根据上下文）。

#### 10.1.6 单文件操作

| 操作 | 行为 |
|------|------|
| 暂存 | 将文件的变更添加到暂存区 |
| 取消暂存 | 从暂存区移除文件的变更 |
| 丢弃 | 将文件的变更恢复到最后提交的状态（触发确认对话框） |

#### 10.1.7 批量操作

| 操作 | 行为 |
|------|------|
| 全部暂存 | 一次性暂存所有未暂存的变更 |
| 全部取消暂存 | 一次性取消暂存所有已暂存的变更 |
| 全部丢弃 | 一次性丢弃所有未暂存的变更（触发确认对话框） |

### 10.2 Git 差异审查

在 Git 变更面板中选中一个文件时，代码编辑器面板切换到差异模式：

- **Monaco 差异编辑器：** 并排视图，左侧显示原始内容，右侧显示修改后的内容。
- **新增行：** 绿色高亮。
- **删除行：** 红色高亮。
- **修改行：** 同时显示删除和添加指示器。

### 10.3 Worktree 检查

一个用于检查仓库关联 Git Worktree 的弹窗。

#### 10.3.1 头部信息

- **Worktree 名称：** 显示为弹窗标题。
- **分支 Chip：** 显示此 worktree 所在的分支。
- **路径 Chip：** 显示 worktree 的文件系统路径。
- **状态 Chip：** 指示 worktree 是 "dirty"（有未提交变更）还是 "clean"。

#### 10.3.2 标签

三个标签用于查看 worktree 的不同方面：

1. **状态标签：**
   - 显示 worktree 路径、分支和状态。
   - 列出各个文件变更及其状态。

2. **差异标签：**
   - 显示 worktree 中变更的文本差异。
   - 以 `<pre>` 块渲染，带有差异格式。

3. **树标签：**
   - worktree 内容的文件树视图。
   - 使用与主文件浏览器相同的 TreeView 组件。

---

## 11. 终端系统

### 11.1 概述

应用提供两种类型的终端：

1. **Agent 终端：** 嵌入在每个 Agent 会话面板中，显示 Agent 的进程输出。
2. **Shell 终端：** 底部面板中的独立终端，用于运行任意命令。

### 11.2 Shell 终端面板

位于工作区页面底部，Agent 面板下方。

#### 11.2.1 布局

- **进度条：** 面板顶部的细线（3px 高度），类似 Agent 面板进度条。
- **终端内容区域：** 活跃终端的 xterm.js 输出。

#### 11.2.2 工具栏

- **小标题：** "TERMINAL"，大写，小字体。
- **终端标题：** 当前终端的名称/标题。
- **终端选择器下拉框：** 列出所有可用 Shell 终端的下拉框。允许在终端之间切换。
- **关闭按钮：** 关闭当前活跃的终端。
- **添加按钮：** 创建新的 Shell 终端标签。

#### 11.2.3 多终端支持

- 用户可以在单个工作区内创建多个 Shell 终端。
- 每个终端是独立的会话，拥有自己的进程历史。
- 终端在选择器下拉框中列出，方便切换。
- 关闭终端会从列表中移除。

#### 11.2.4 空状态

当不存在终端时，面板显示：
- 一条消息："No terminal yet"
- 一个创建新终端的操作按钮

### 11.3 Agent 终端

每个 Agent 会话有自己的嵌入终端：

- **类名：** `.agent-pane-xterm`
- **渲染模式：**
  - **直接 xterm：** 通过 xterm.js 的标准终端渲染。
  - **转录模式：** 隐藏的 xterm 加上可见的 `<pre>` 文本覆盖层，以提高历史输出的可读性。
- **交互模式：**
  - **交互：** 用户可以在终端中输入命令。
  - **只读：** 终端仅显示输出；不接受用户输入。

### 11.4 终端样式

| 属性 | 值 |
|------|-----|
| 背景 | `#0b1218` |
| 前景文本 | `#d8e2ea` |
| 光标颜色 | `#78d7b2`（绿色） |
| 选区背景 | `rgba(108, 182, 255, 0.24)`（蓝色色调） |
| 字体 | JetBrains Mono，13px |
| 滚动条 | 3px 宽，圆角手柄 |

### 11.5 终端兼容模式

在设置 → 外观中，用户可以选择两种终端渲染模式：

- **标准：** 默认渲染模式，针对性能优化。
- **兼容：** 替代渲染模式，适用于标准模式存在显示问题的环境。

### 11.6 终端大小

- 终端自动适配其容器大小。
- 终端大小被追踪并在工作区重新打开时恢复。

---

## 12. 命令面板

### 12.1 访问方式

- **键盘：** `Ctrl/Cmd + K`
- **按钮：** 顶栏中的 "Quick Actions" 按钮

### 12.2 布局

一个模态对话框（最大宽度 660px，8px 圆角），紧凑密度布局。

#### 12.2.1 头部

- **小标题：** "COMMAND PALETTE"
- **元文字：** 显示可用操作的数量（如 "12 actions"）

#### 12.2.2 搜索行

- **搜索图标：** 左侧。
- **搜索输入：** 实时过滤操作列表。
- **提示文字：** 输入框下方的引导文字。

#### 12.2.3 结果列表

- 列出所有匹配搜索的操作。
- 每个操作项显示：
  - **标签：** 操作名称。
  - **描述：** 操作功能的简短说明。
  - **快捷键徽标（可选）：** 操作的键盘快捷键，以右侧的小标签显示。

#### 12.2.4 空状态

当没有操作匹配搜索时："No results found"

### 12.5 可用操作

| 操作 | 描述 | 快捷键 |
|------|------|--------|
| 新建工作区 | 打开新工作区标签 | `Ctrl/Cmd + N` |
| 切换专注模式 | 进入或退出专注模式 | `F` |
| 切换代码面板 | 显示或隐藏代码编辑器面板 | — |
| 切换终端面板 | 显示或隐藏终端面板 | — |
| 聚焦 Agent 输入 | 聚焦活跃 Agent 的输入区域 | — |
| 垂直分割面板 | 将活跃 Agent 面板分割为左右面板 | `Cmd/Ctrl + D` |
| 水平分割面板 | 将活跃 Agent 面板分割为上下面板 | `Shift + Cmd/Ctrl + D` |
| 切换到上一个工作区 | 移动到左侧的工作区标签 | `Ctrl/Cmd + Shift + [` |
| 切换到下一个工作区 | 移动到右侧的工作区标签 | `Ctrl/Cmd + Shift + ]` |
| 打开设置 | 导航到设置页面 | — |
| 切换到 [工作区名称] | 直接切换到特定工作区标签 | — |

### 12.6 交互

- **键盘导航：** 上箭头/下箭头在操作列表中移动，Enter 激活聚焦的操作，Escape 关闭面板。
- **鼠标导航：** 点击任意操作以激活。
- **关闭：** 点击面板外部或按 Escape 关闭。

---

## 13. 设置

### 13.1 访问方式

- **按钮：** 顶栏中的齿轮图标
- **链接：** 欢迎页面上的 "Open Settings"
- **命令面板：** "Open Settings" 操作

### 13.2 布局

专用页面，两列布局：

- **侧边栏导航（200px 宽）：** 列出所有设置分区。
- **内容区域：** 显示所选分区的设置。

### 13.3 导航分区

| 分区 | 图标 | 描述 |
|------|------|------|
| 通用 | 设置图标 | 默认 Agent Provider、通知偏好 |
| Claude（每个 Provider） | 配置图标 | Claude 专属设置 |
| Codex（每个 Provider） | 配置图标 | Codex 专属设置 |
| 外观 | 外观图标 | 主题、终端渲染、语言 |

每个导航项至少 42px 高，14px 内边距，7px 圆角。活跃分区以强调色背景和边框高亮。

### 13.4 通用设置

#### 13.4.1 Agent 默认值

- **默认 Provider：** 药丸选择器（一组切换按钮），列出可用 Provider（Claude、Codex）。决定创建新 Agent 会话时预选的 Provider。

#### 13.4.2 完成通知

- **完成通知开关：** 启用或禁用 Agent 会话完成时的声音和浏览器推送通知。
- **仅在后台通知开关：** 启用时，仅在应用窗口不是活跃/聚焦窗口时触发通知。
- **通知权限状态：** 静态文字显示浏览器是否已授予通知权限（如 "已授予"、"已拒绝"、"默认"）。

### 13.5 Provider 设置（每个 Provider）

每个 Provider 有自己的设置面板，包含：

#### 13.5.1 摘要卡片

一张卡片，显示 Provider 的徽章标签和当前配置摘要。

#### 13.5.2 注入 Hooks 分区

- **内联按钮：** 将 Provider 专属 hooks 注入到活跃工作区的按钮。
- **状态显示：** 注入尝试后显示成功或错误状态。

#### 13.5.3 配置字段

基于 Provider 配置模式动态生成字段：

| 字段类型 | UI 组件 |
|----------|---------|
| 字符串 | 单行文本输入 |
| 字符串列表 | 文本区域（每行一个项目） |
| 环境变量映射 | 文本区域（key=value 对） |
| JSON | 文本区域，JSON 内容 |
| 下拉选择 | 下拉菜单 |

每个字段可能有：
- **标签：** 字段名称。
- **描述：** 解释字段用途的帮助文字。
- **错误显示：** 校验失败时字段下方的红色文字。

#### 13.5.4 命令预览

一个显示区域，展示启动 Agent 进程时将使用的有效命令，包含所有当前设置值。修改设置时实时更新。

### 13.6 外观设置

#### 13.6.1 主题

- **当前状态：** 仅深色主题。
- **UI：** 药丸选择器显示 "Dark" 为活跃选项。浅色主题已计划但当前未发布。

#### 13.6.2 终端渲染

- **选项：** "Standard" / "Compatibility"
- **UI：** 药丸选择器（切换按钮）。

#### 13.6.3 语言

- **选项：** "English" / "Chinese"
- **UI：** 药丸选择器（切换按钮）。
- **行为：** 选择后立即切换界面语言。偏好设置会被持久化，在后续访问中生效。

### 13.7 页脚栏

在设置页面底部：

- **自动保存提示：** 告知用户设置会自动保存的消息。
- **构建元数据：** 应用版本号和构建发布时间戳。

---

## 14. 专注模式

### 14.1 用途

专注模式通过隐藏非必要的 UI 元素来提供抗干扰环境，让用户可以专注于 Agent 输出和代码。

### 14.2 激活方式

- **键盘：** 按 `F` 键（未聚焦于文本输入框时）。
- **命令面板：** "Toggle Focus Mode" 操作。

### 14.3 行为

激活专注模式时：

- 顶栏被隐藏或最小化。
- 左侧边栏（代码面板）被隐藏。
- 底部终端面板被隐藏。
- Agent 工作区（中央面板）扩展以填充可用空间。
- Agent 面板头部可能被简化。

### 14.4 退出方式

- **键盘：** 按 `Escape`。
- **命令面板：** 再次执行 "Toggle Focus Mode" 操作。

所有隐藏的 UI 元素恢复到之前的状态。

---

## 15. 通知系统

### 15.1 用途

在长时间运行的 Agent 会话完成工作时通知用户，尤其是在用户在其他标签页或应用中工作时。

### 15.2 通知类型

#### 15.2.1 声音通知

- Agent 会话转换到完成状态时播放任务完成声音（`task-complete.wav`）。
- 可在设置 → 通用 → 完成通知中启用/禁用。

#### 15.2.2 浏览器推送通知

- 显示浏览器推送通知，包含：
  - **标题：** 指示会话/工作区名称。
  - **正文：** 关于完成的消息（如 "会话已完成"）。
- **点击行为：** 点击通知会将用户视图切换到相关的工作区和会话。
- 可在设置 → 通用 → 完成通知中启用/禁用。

### 15.3 通知配置

| 设置 | 选项 | 默认值 |
|------|------|--------|
| 完成通知 | 启用 / 禁用 | 启用 |
| 仅在后台通知 | 启用 / 禁用 | 启用 |

---

## 16. Supervisor 系统

### 16.1 用途

Supervisor 是一个自动化评估系统，定期评估 Agent 会话朝着定义目标的进展情况。它可以根据会话状态注入指导、暂停评估或触发重新评估。

### 16.2 Supervisor 状态

| 状态 | 描述 |
|------|------|
| **未激活** | 会话未配置 Supervisor。 |
| **空闲** | 已配置 Supervisor 但当前未评估。 |
| **评估中** | Supervisor 正在积极评估会话进度。 |
| **注入中** | Supervisor 正在向会话注入指导或反馈。 |
| **已暂停** | Supervisor 评估暂时挂起。 |
| **错误** | Supervisor 在评估过程中遇到错误。 |

### 16.3 Supervisor UI

#### 16.3.1 Agent 面板中的 Supervisor 分区

每个带有活跃 Supervisor 的 Agent 会话显示 Supervisor 卡片（`.agent-pane-supervisor-card`），包含：

- **状态属性：** 卡片反映当前 Supervisor 状态（evaluating、injecting、paused、error、off）。
- **标签行：**
  - 徽章勾选图标 + "Supervisor" 标签。
  - 状态标签显示当前状态。

#### 16.3.2 Supervisor 操作

当会话存在 Supervisor 时，可用操作按钮（13px 图标）：

| 按钮 | 图标 | 操作 |
|------|------|------|
| 编辑目标 | 消息方块 | 打开 Supervisor 目标对话框以修改评估标准 |
| 暂停 | 圆形暂停 | 暂停 Supervisor 评估 |
| 恢复 | 播放 | 恢复已暂停的 Supervisor |
| 重试 | 刷新 | 重新尝试失败的评估 |
| 触发 | 上箭头 | 手动触发评估周期 |
| 禁用 | 方形 | 禁用此会话的 Supervisor |

#### 16.3.3 启用 Supervisor

当会话未配置 Supervisor（且会话不在草稿状态）时：

- 显示"启用"按钮（播放图标）。
- 点击以"启用"模式打开 Supervisor 目标对话框。

### 16.4 Supervisor 目标对话框

用于定义和编辑 Supervisor 评估标准的模态对话框。

#### 16.4.1 模式

- **启用：** 首次设置 Supervisor。
- **编辑：** 修改现有 Supervisor 目标。
- **禁用：** 从会话中移除 Supervisor。

#### 16.4.2 布局

- **目标文本区域：** 多行文本区域（5 行，自动聚焦），占位符提示用户描述目标。
- **预览分区：** 只读预览，以格式化代码块（`<pre>`）显示目标文字。
- **操作按钮：**
  - **取消：** 关闭对话框，不保存。
  - **确认：** 主要按钮，保存目标并激活/更新 Supervisor。

### 16.5 Supervisor 周期

每次评估周期以下列状态追踪：

| 状态 | 描述 |
|------|------|
| 排队 | 等待评估 |
| 评估中 | 当前正在评估 |
| 已完成 | 评估成功完成 |
| 已注入 | 已向会话注入指导 |
| 失败 | 评估失败 |

---

## 17. 国际化

### 17.1 支持的语言

| 语言 | 代码 |
|------|------|
| 英语 | `en` |
| 中文（简体） | `zh` |

### 17.2 语言切换

- 可以在设置 → 外观 → 语言中切换语言。
- 选择后界面立即更新。
- 偏好设置保存在用户设置中，在后续访问时生效。

### 17.3 本地化内容

以下内容类型已本地化：

- 所有 UI 标签、按钮文字和导航元素。
- 会话标题（由 Agent 生成）。
- 工作区标题。
- 终端标题。
- 相对时间格式（如 "2 分钟前"、"3 小时前"）。
- 小标题和分区标签。

### 17.4 翻译系统

- 使用基于键的翻译系统，支持 `{key}` 插值。
- 翻译文件以 JSON 格式存储（`locales/en.json`、`locales/zh.json`）。
- `createTranslator` 函数提供运行时翻译查找。

---

## 18. 设计系统

### 18.1 组件密度

应用在所有表面使用**紧凑**密度布局。带有 `data-density="compact"` 的组件比舒适密度变体具有更紧的内边距和更小的字号。

### 18.2 按钮样式

| 样式 | 类名 | 用途 |
|------|------|------|
| 主要 | `.btn.btn-primary` | 确认操作、启动操作 |
| 默认 | `.btn` | 取消、次要操作 |
| 危险 | `.btn.btn-danger` | 破坏性操作（丢弃、删除） |
| 图标按钮 | `.pane-action` | 面板级操作（分割、关闭） |
| 工具栏按钮 | `.topbar-tool` | 顶栏操作按钮 |

### 18.3 交互状态

| 状态 | 处理 |
|------|------|
| 悬停 | 背景变为较亮色调（通过 `color-mix` 计算） |
| 活跃/按下 | 背景进一步变暗，轻微缩小 |
| 聚焦 | 强调色轮廓 |
| 禁用 | 降低不透明度（50%），pointer-events none |

### 18.4 面板缩放器

- **宽度：** 主面板分隔条 4px，Agent 分割分隔条 8px。
- **光标：** 垂直分隔条 `col-resize`，水平分隔条 `row-resize`。
- **悬停状态：** 背景变为强调色（`#6cb6ff`）。
- **调整大小时：** 面板内容上的所有过渡禁用（`transition: none !important`）以保证性能。`body.is-resizing-panels` 类应用于 body 元素。

### 18.5 模态弹窗

- **遮罩：** 半透明黑色遮罩（`rgba(0, 0, 0, 0.5)`）。
- **卡片：** 从 0.95 缩放入场，带淡入动画。
- **关闭：** 点击模态外部、按 Escape 或点击关闭按钮。
- **密度：** 所有模态使用紧凑密度。

### 18.6 状态徽标与指示器

#### 18.6.1 会话状态点

| 颜色 | 状态 |
|------|------|
| 绿色（`#78d7b2`） | 活跃/运行中 |
| 灰蓝色 | 空闲 |
| 蓝色（`#6cb6ff`） | 排队 |
| 弱化 | 关闭/不可用 |

#### 18.6.2 源码状态徽标

| 颜色 | 状态 |
|------|------|
| 绿色（`#78d7b2`） | 已暂存 |
| 橙色（`#f1b86a`） | 已修改 |
| 蓝色（`#6cb6ff`） | 未跟踪 |
| 红色（`#ff9eb0`） | 已删除 |

#### 18.6.3 Worktree 状态 Chips

| 类名 | 状态 |
|------|------|
| `.worktree-meta-chip.status.dirty` | 有未提交变更 |
| `.worktree-meta-chip.status.clean` | 无待处理变更 |

### 18.7 进度条

- **高度：** 3px。
- **行为：** 宽度根据完成率动画变化。
- **颜色：**
  - 活跃/加载中为强调蓝色。
  - 错误状态为危险粉色。
- **最小宽度：** 6%（即使 0% 进度也始终可见）。
- **运行状态：** 活跃运行时最小 34% 宽度。
- **过渡：** 宽度以快速时长（60ms）动画。

---

## 19. 交互模式

### 19.1 悬停显示模式

多个 UI 元素遵循悬停显示模式，其中次要操作默认隐藏，仅在悬停时出现：

- **标签关闭按钮：** 工作区标签上的 X 按钮默认隐藏，直到悬停标签。
- **面板操作按钮：** Agent 面板头部中的分割和关闭按钮在悬停时出现。
- **文件夹浏览器操作：** 悬停目录行时出现"进入文件夹"操作。

### 19.2 药丸选择器模式

在离散值之间选择的设置选项使用药丸选择器（一行切换按钮）：

- 一次只有一个药丸活跃（单选）。
- 活跃药丸具有不同的背景和边框。
- 点击非活跃药丸激活它并取消之前的选择。
- 用于：默认 Provider、主题、终端渲染、语言。

### 19.3 确认对话框模式

破坏性操作（全部丢弃、关闭有活跃会话的工作区）触发确认对话框：

- **遮罩：** 半透明背景。
- **卡片：** 紧凑模态卡片，包含：
  - **头部：** H3 标题。
  - **主体：** 消息文字，带有可选详情分区（内容 + 时间戳）。
  - **页脚：** 取消按钮（默认样式）和确认按钮（危险样式）。

### 19.4 空状态模式

应用每个主要分区都定义了空状态：

| 区域 | 空状态内容 |
|------|-----------|
| 顶栏（无工作区） | 小标题 + 标签提示打开工作区 |
| 欢迎页面 | 产品描述 + 操作按钮 |
| 终端面板（无终端） | "No terminal yet" + 添加按钮 |
| Agent 面板（草稿） | Provider 选择按钮 |
| Agent 面板（不可用） | 错误标题、原因和移除按钮 |
| Agent 面板（无输出） | "No agent output yet" |
| 命令面板（无结果） | "No results found" |
| 文件搜索（无匹配） | 空下拉框 |

### 19.5 加载状态模式

- **运行时校验：** 检查时状态点蓝色脉冲动画。
- **认证页面：** 加载中状态时显示旋转加载器。
- **骨架屏：** 初始加载期间占位条闪烁动画。
- **Agent 会话：** 初始化期间进度条最小 6% 宽度。

### 19.6 实时同步

应用保持以下内容的实时同步：

- Agent 事件（会话启动、一轮完成）。
- 终端事件（输出片段）。
- 生命周期事件（工作区状态变更）。
- 制品脏事件（Git 变更、Worktree 变更、文件树变更）。

连接中断时自动重连，带有回退机制。

---

## 20. 边界情况与错误状态

### 20.1 认证错误

| 场景 | 行为 |
|------|------|
| 密码不正确 | 输入框下方显示错误消息；用户可以立即重试 |
| 会话过期 | 用户被重定向到认证页面，显示过期消息 |
| IP 被封 | 显示"已封锁"状态，附带锁定持续时间信息 |
| 认证服务不可用 | 显示"不可用"状态，带有重试选项 |

### 20.2 Agent 会话错误

| 场景 | 行为 |
|------|------|
| Agent 进程启动失败 | 面板进入"不可用"状态，显示错误原因和移除按钮 |
| Agent 进程崩溃 | 终端显示崩溃输出；面板状态反映错误 |
| Agent 会话超时 | 会话标记为空闲；可能触发完成提醒 |

### 20.3 工作区错误

| 场景 | 行为 |
|------|------|
| 目录无法访问 | 工作区启动失败；在启动浮层中显示错误消息 |
| 运行时校验失败 | 工作区启动被阻止；运行时校验浮层显示缺失命令及安装提示 |
| 工作区恢复失败 | 工作区以干净状态打开；之前的会话数据可能丢失 |

### 20.4 Git 错误

| 场景 | 行为 |
|------|------|
| 非 Git 仓库 | Git 变更面板显示空状态，提供初始化 Git 的引导 |
| Git 命令失败 | 错误显示在相关分区（如提交失败） |
| 合并冲突 | 有冲突的文件在变更列表中高亮 |

### 20.5 终端错误

| 场景 | 行为 |
|------|------|
| PTY 分配失败 | 终端显示错误消息和重试选项 |
| 终端进程意外退出 | 终端显示退出码和创建新终端的提示 |

### 20.6 并发冲突

| 场景 | 行为 |
|------|------|
| 两个标签页尝试控制同一工作区 | 控制器/观察者协议确保只有一个标签页有写入权限；另一个标签页观察 |
| 控制器心跳错过截止时间 | 观察者启动基于截止时间的交接协议进行接管 |
| 活跃会话期间页面卸载 | 控制器优雅释放；工作区状态保留 |

### 20.7 网络中断

| 场景 | 行为 |
|------|------|
| WebSocket 断开 | 自动重连，带有回退；断开连接期间事件缓冲 |
| 服务器无法访问 | Agent 会话在本地继续运行；UI 显示连接状态 |

### 20.8 文件系统错误

| 场景 | 行为 |
|------|------|
| 文件被外部删除 | 文件树通过文件观察器自动更新；编辑器中打开的文件显示过期状态直到刷新 |
| 文件权限被拒绝 | 尝试打开或保存文件时显示错误 |

---

## 附录 A：当前边界（尚未发布的功能）

以下功能已部分实现或已计划，但在当前版本中尚未完全可用：

| 功能 | 当前状态 |
|------|----------|
| 多 Agent Provider 支持 | Claude 和 Codex 存在，但多 Provider 编排尚未完全就绪 |
| 浅色主题 | 仅深色主题；浅色主题已计划但未发布 |
| 完整可视化任务队列 UI | 任务队列在内部存在，但未在 UI 中完全可视化 |
| 完整归档/调度中心 | 归档历史存在，但完整管理中心未发布 |
| 显式 Worktree 管理入口 | Worktree 检查存在，但完整创建/删除管理不可用 |
| 完全闭环自动暂停行为 | 空闲策略存在，但自动暂停尚未完全自动 |
| 远程 Git 工作区创建 | 后端支持，但 UI 入口已隐藏/延期 |

---

## 附录 B：产品指标与遥测（推荐）

应跟踪以下指标以了解产品使用情况：

| 指标 | 用途 |
|------|------|
| 每个会话的活跃工作区数 | 了解典型使用深度 |
| 每个工作区的 Agent 会话数 | 了解并行需求 |
| 平均会话时长 | 了解典型 Agent 运行时长 |
| Supervisor 激活率 | 衡量 Supervisor 功能的采用情况 |
| 专注模式使用率 | 衡量对抗干扰模式的需求 |
| 每个工作区的终端数 | 了解 Shell 终端需求 |
| Git 提交频率 | 了解 Git 集成使用情况 |
| 命令面板使用频率 | 衡量可发现性和采用率 |
| 设置变更频率 | 了解配置摩擦 |
| 认证失败率（远程部署） | 监控认证健康状态 |

---

## 附录 C：无障碍考量

| 领域 | 建议 |
|------|------|
| 颜色对比度 | 确保所有文字满足 WCAG 2.1 AA 对比度要求（正常文字 4.5:1，大文字 3:1） |
| 键盘导航 | 所有交互元素必须可通过键盘到达和操作 |
| 聚焦指示 | 所有可聚焦元素必须有可见的聚焦指示器 |
| 屏幕阅读器支持 | 应为纯图标按钮和复杂交互组件添加 ARIA 标签 |
| 运动偏好 | 偏好减少动画的用户应能禁用脉冲动画和过渡 |

---

*文档结束*
