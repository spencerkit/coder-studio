# Tooltip

Bounded desktop/fine-pointer tooltip wrapper. It clones exactly one trigger element, renders
text-only tooltip content into `document.body`, and becomes a no-op wrapper on coarse/mobile
viewports in this phase.
