# Drawer

Shared governed desktop drawer for large detail views, multi-section editing, and context-preserving
workflows. It portals to `document.body`, traps focus, locks document scroll while open, and keeps
backdrop dismissal opt-in via `backdropDismiss` so interactive business surfaces do not close on
accidental outside clicks.
