# Pill

Shared pill-style button primitive for compact option selection.

## Notes
- Token-backed styling with legacy `settings-pill*` compatibility classes preserved.
- Current bounded callers: settings appearance option groups.
- The primitive owns its styles locally and only emits the legacy hook classes on its rendered button.
