# Tag

Shared text label primitive for small status and metadata tags.

## Notes
- Token-backed styling with legacy `badge` and `badge-*` compatibility classes preserved.
- Current bounded callers: session labels, branch quick pick remote label, mobile select item badge.
