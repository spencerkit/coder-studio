# SegmentedControl

Option-driven segmented selector built on shared `Tabs` semantics.

- Import from `src/components/ui/index.ts` only.
- Use for compact controlled selectors with `options`, `value`, `onChange`, and optional `size`.
- Preserve legacy compatibility classes with `className` and `optionClassName` during bounded migrations.
